/* ============================================================
   config.secret.example.js — API 키 설정 템플릿

   사용 방법:
     1. 이 파일을 복사해서 같은 폴더에 config.secret.js 로 저장
     2. 아래 '' 안에 Google AI Studio API 키 입력
     3. config.secret.js 는 .gitignore 에 등록되어 있으므로 커밋되지 않음

   ※ config.secret.js 가 없어도 게임은 정상 작동 (내장 AI 사용)
   ============================================================ */

CONFIG.GEMINI_API_KEY = '';   // ← 여기에 키 입력: 'AIzaSy...'
